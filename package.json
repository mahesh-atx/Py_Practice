{
  "name": "pypractice-platform",
  "version": "2.0.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build:notes": "node scripts/build-notes.js",
    "test": "node --test tests/*.test.js",
    "test:verbose": "node --test --test-reporter=spec tests/*.test.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
